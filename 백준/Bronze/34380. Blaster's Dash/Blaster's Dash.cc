#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long M, V, n, K;
    cin >> M >> V;
    cin >> n;
    cin >> K;

    cout << 2 * n + 40 << "\n";
    return 0;
}
